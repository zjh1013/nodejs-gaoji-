#!/usr/bin/env node
const program=require('commander');
const inquirer=require('inquirer');
const package = require('./packaget.json')
const actionHandler=()=>{
    const question=[
        {
            type:'input',
            name:"username",
            message:'请输入你的名字'
        },
        {
            type:'list',
            choices:['male','female'],
            message:'选择性别'
        },
        {
           type:'input',
           name:'address',
           message:'请输入你家庭住址'
        },
        {
            type:'checkbox',
            name:'hobby',
            message:'请选择你的爱好',
            choices:['吃饭','睡觉','游戏']
        },
        {
            type:'confirm',
            name:'confirm',
            message:'确定要提交🐎'
        }
    ];
    inquirer.prompt(question).then(res=>{
        if(res.confirm){
            console.log(res)
        }else{
            console.log('取消!')
        }
    })
};

program
     .version(package) 
     .command('init')
     .action(actionHandler);
     program.parse(process.argv);
